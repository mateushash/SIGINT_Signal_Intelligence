LISTENER:
  - Monitors a specific Frequency 
  - Captures and logs signal data for analysis
  - Sends captured data to central database
  - Has conection with other signal nodes and can share data between them as well as know if its offline

BUFFER:
  - Stores and makes logs of caputred data:
    - Timestamp
    - Frequency
  - Responsible for data integrity and backup
    - Removes Duplicates
    - Keeps a message buffer that will be transfered to the Decryption Nodes

WORKER:
  - Receives captured signal data 
  - Has to transform the data into a meaningull and cohereent information.
  - Uses various decryption algorithms to decode the captured signals
    -Each node could try a diferent cypher configurations
    -Each node could try diferent keys
  - Sends decrypted information to the Analysis Node for further processing

  Attributes:

MAIN - Command Center (Coordinator)
  - Central Node 
  - Assign decoding tasks
  - Track worker(decoder) progress
  - Collects Successfull Decreyption

Signal Inteligence (SIGINT)


CIFRA: pode ser qualquer forma de decriptografar uma mensagens, hence código morse ou cseasar cipher

************************************
**********DATA ABSTRACTION**********
************************************

Worker
  type based on the cypher 
    w1: morse 
    w2: vigenere 
    w3: vigenere
  constructor must initialize it based on the type of cypher
  must have a string to work it(pass the &object and check)
  
  has the message buffer 
  receives a part of the buffer, decodes it and is deleted
  
Cypher:
  whats a cypher
    key: 
      - morse, ceasar, vigenere 

Morse: 
  Binary version: 
    0: space inter element character 
    000: space between letters 
    000 0 000: space between words
    1: dot 
    111: dash

    A = *_   ->  1 0 111  
    B = _*** -> 111 0 1 0 1   
    EU = * **_ -> 1 000 1010111


/************************************************************************/
/************************ Funcao teste de string ************************/
/************************************************************************/
1. ABSTRACAO
  A mensagem m contem com certeza uma frase completa(não necessariamente coerente), sem erros ortograficos ou de perda de dado e
sem espacos.

Exemplos:

CORRETOS
 a. "AMARÉVIDA"
 b. "AMORAPESSOA" note que é "AMOR" e não "AMORA" o algo deve pegar a priemeira ocorrencia
 c. "AAULADEECONOMIADEQUARTAÉTOPDEMAIS"
 d. "UMCOPODECAFÉDEMANHÃOUUMTIRÃONACABEÇA"

ERRADOS
  a. "AMAR É VIDA" contem espacos
  b. "AMOVC"  contem abreviacoes
  c. "SHEREKÉVIDA" contem nome proprio

2. OBJETIVO
  Identificar se a mensagem contem apenas palavras, para isso encontrar uma palavra, bater no dicionario de lingua portuguesa e se tiver uma entrad nele, addicionar a palavra na string que representa a mensagem final e continuar procurando outras palavras na string original até chegar ao seu final.

3. SUGESTAO DE ALGORITMO
  Andar letra por letra até encontrar uma palavra, quando achar, remover os caracteres encontrados da mensagem original e adicionar a palavra encontrada na string que representa a mensagem final "decodificada".

iteraçao 1
string m = "AMORAOPRÓXIMO" -> (AMOR AO PROXIMO)
palavra p = A 
  p é palavra ? remove p de m : addciona o proximo caracter a p

  .
  .
  .

iteracao 4 
string m = "AMORAOPRÓXIMO" -> (AMOR AO PROXIMO)
palavra p = amor 
  p é palavra ? nesse caso sim, então remover p de m 

iteracao 5
m = "AOPROXIMO"
p = 'A'
  p é palavra ? remove p de m : addciona o proximo caracter a p

4. TESTES DE CASO
CASO 1: 
  m = AMARAOPRÓXIMO
  p1 = AMAR 
  p2 = AO 
  p3 = PRÓXIMO

/************************************************************************/
/*********************** BANCO DE DADOS DO BUFFER ***********************/
/************************************************************************/
1. ESTRUTURA DO BANCO
Registro: parte da mensagem que foi entregue para decodificacao, cada registro é composto pelos seguintes atributos

Atributos:
  1 Timestam recebimento
  2 pedaco da mensagem original 
  3 nó worker para qual foi enviado 
  4 tipo de cyfra que esta sendo tentada 
  5 timestamp de envio para o noh, para que se possa contar quanto tempo esse noh esta tentado essa cifra
  6 mensagem decodificada que sera retornada pelo trabalhador

registro  timestamp de |  pedaco da mensagem  |   nó worker para  |  cifra  | timestamp de envio | mensagem 
          recebimento  |           original   |  qual foi enviado |         |  (conter statusa)  | decodificad

2. IMPLEMENTACAO
  Banco em sql que armazenara os registros para que os buffers possam checar e API em python para interaçcao entre com o banco pelo buffer

  Soh é necessario um crud basico em python com os atributos da estrutura 
  CREATE
  READ 
  UPDATE
  DELETE 

