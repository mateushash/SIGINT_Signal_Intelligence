#include <string>

#include "MorseCode.h"

// Suppose the message is 100% sure in morse code with 1 being dots and 0 being dashes
std::string MorseCode::decodeMessage(std::string m) {


  return m;
}
